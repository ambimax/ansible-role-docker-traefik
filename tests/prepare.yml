---
- hosts: localhost
  remote_user: root

  vars:
    docker_install: true

  pre_tasks:
    - name: Install dependencies
      apt:
        pkg:
        - curl
        - git
        update_cache: true

  roles:
    - ambimax.docker
